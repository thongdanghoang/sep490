package enterprise;

import org.springframework.boot.SpringApplication;

public class EnterpriseApplication {
    
    public static void main(String[] args) {
        SpringApplication.from(EnterpriseApplication::main).run(args);
    }
    
}
